%section
	.wrapper	
		%h1
			Post Index Page
	%hr/
	-@post.each do |post|
		.postwrap.clearfix
			.wrapper
				%h2.title= link_to post.title, post
				%p
					Published on 
					=post.created_at.strftime("%D at %T")
					


		